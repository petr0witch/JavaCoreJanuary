# Используем базовый образ с установленной JDK
FROM openjdk:11

# Копируем исходный код в контейнер
COPY . /app

# Устанавливаем рабочую директорию
WORKDIR /app

# Генерируем документацию
RUN javadoc -d docs -sourcepath src

# Устанавливаем любые другие зависимости, необходимые для вашего проекта

# Команда, которая будет выполнена при запуске контейнера (может быть пустой)
CMD ["echo", "Documentation generated successfully!"]
